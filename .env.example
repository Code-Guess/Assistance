# OpenRouter API key used to call the tutoring model.
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Render sets PORT automatically in production; only needed for local runs.
PORT=8080
